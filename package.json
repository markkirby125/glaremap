{
  "name": "glaremap",
  "version": "0.1.0",
  "description": "Spatial glare heatmap + targeted softening for photophobia — lab page and bookmarklet.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node build.js"
  }
}
